export default function Navbar() {
  return (
    <nav className="relative z-20 flex h-[clamp(2.55rem,5.675vw,4.675rem)] flex-1 items-center justify-end rounded-full bg-[#dedede] px-[clamp(2rem,4.25vw,5.5rem)]">
      <button className="font-bricolage text-[clamp(2.2rem,3.05vw,4.25rem)] font-extrabold leading-none tracking-[-0.015em] text-black">
        Menu
      </button>
    </nav>
  );
}
